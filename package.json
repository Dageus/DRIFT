{
  "name": "@drift-network",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^25.9.1"
  },
  "scripts": {
    "build": "npm run build --workspaces"
  }
}
