// Attestation providers — sources of raw AttestationRecords (EAS, Verax, ...). Import from
// '@drift-network/sdk/providers' rather than the package root.
export { EASProvider } from './EAS.js';
export type { IAttestationProvider } from './IAttestationProvider.js';
